import SwiftUI

struct ProgressPage: View {
    
    var body: some View {
        Text("progress page")
            .font(.title)
            .bold()
            .multilineTextAlignment(.center)
    }
}
